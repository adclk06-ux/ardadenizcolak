/**
 * Metadata helpers — re-exported from siteConfig.ts
 * See siteConfig.ts for the single source of truth.
 */

export { siteConfig as site, pageTitle, fullUrl, metaDescription } from "./siteConfig";
